#incluse "main.h"
 
/**
 *print_alphabet - prints abc in lower case
 *
 *Return: void
 */
 
void print_alphabet(void)
	 
{
	        int sh;
	 
		for (sh = 'a'; sh <= 'z'; sh++)
		{
			        _putchar(sh);
		}
	_putchar('\n');
}
